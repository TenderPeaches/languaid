<%= form_with(model: adjective) do |form| %>
  <% if adjective.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(adjective.errors.count, "error") %> prohibited this adjective from being saved:</h2>

      <ul>
        <% adjective.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
