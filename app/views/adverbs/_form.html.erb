<%= form_for :adverb, { url: adverbs_path } do |form| %>
  <% # render "common/form_errors", model: @adverb %>

  <%= render "words/fields", form: form, wordable_type: "adverb" %>
  
  <div>
    <%= form.submit %>
  </div>
<% end %>