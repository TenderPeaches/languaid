<%= form_with(model: preposition) do |form| %>
  <% if preposition.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(preposition.errors.count, "error") %> prohibited this preposition from being saved:</h2>

      <ul>
        <% preposition.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
