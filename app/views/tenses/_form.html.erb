<%= form_with(model: tense) do |form| %>
  <% if tense.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tense.errors.count, "error") %> prohibited this tense from being saved:</h2>

      <ul>
        <% tense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
