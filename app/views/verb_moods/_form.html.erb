<%= form_with(model: verb_mood) do |form| %>
  <% if verb_mood.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(verb_mood.errors.count, "error") %> prohibited this verb_mood from being saved:</h2>

      <ul>
        <% verb_mood.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
