<%= form_with(model: word_definition) do |form| %>
  <% if word_definition.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(word_definition.errors.count, "error") %> prohibited this word_definition from being saved:</h2>

      <ul>
        <% word_definition.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :definition, style: "display: block" %>
    <%= form.text_field :definition %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
